fastapi==0.136.1
uvicorn[standard]==0.32.1
asyncpg==0.29.0
python-dotenv==1.2.2
pydantic==2.10.3
databases==0.9.0
PyJWT==2.12.0
httpx==0.27.2
aiohttp==3.13.4
dnspython==2.7.0
boto3==1.35.99
bcrypt==4.2.0
cryptography==46.0.7
# Pinned via fastapi's transitive requirement (>=0.46.0). The earlier
# explicit `starlette==1.0.1` was unsatisfiable (fastapi requires <1.0.0 in
# this version range) and was silently ignored by `pip install`. Letting
# pip-tools resolve it produces an actually-installable lockfile.
starlette>=0.46.0,<1.0.0
google-re2==1.1.20251105
alembic==1.14.0
psycopg2-binary==2.9.10
prometheus-fastapi-instrumentator==7.0.0
