fastapi==0.136.1
uvicorn[standard]==0.32.1
asyncpg==0.29.0
python-dotenv==1.2.2
pydantic==2.10.3
databases==0.9.0
PyJWT==2.12.0
httpx==0.27.2
aiohttp==3.13.4
dnspython==2.7.0
bcrypt==4.2.0
cryptography==46.0.7
# F-53 / PYSEC-2026-161 — starlette 1.0.1 fixes a vulnerability present in
# 0.52.x. FastAPI 0.136.x supports starlette 1.x. Pin exact to keep the
# lockfile reproducible.
starlette==1.0.1
google-re2==1.1.20251105
alembic==1.14.0
psycopg2-binary==2.9.10
# F-45 — Prometheus exposition. Using prometheus_client directly (not
# prometheus-fastapi-instrumentator) because the instrumentator caps
# starlette<1.0.0 and starlette 0.52.x has PYSEC-2026-161 (fix in 1.0.1).
# Direct exposition is ~30 lines and matches what the instrumentator wraps.
prometheus-client==0.21.0
# F-49 — per-customer rate limiting backed by Redis token-bucket.
# Async client; redis-py 5.x rolls hiredis + asyncio support into the main pkg.
redis==5.2.1
# F-58 — Sentry runtime error monitoring. SDK with FastAPI integration.
# No-op when SENTRY_DSN is unset, so safe to keep installed in all envs.
sentry-sdk[fastapi]==2.60.0
