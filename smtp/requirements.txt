aiosmtpd==1.4.6
asyncpg==0.29.0
python-dotenv==1.2.2
boto3==1.35.0
aiohttp==3.13.4
cryptography==46.0.7
requests==2.34.2
google-re2==1.1.20251105
# S-M6 — distributed sliding-window rate limiter (shared with backend pin).
redis==5.2.1
# S-H1 — anti-spoofing: SPF check on port-25 inbound (defense-in-depth on
# top of the peer-IP allowlist). pyspf does the RFC 7208 lookups; dnspython
# is its preferred resolver and we already use it elsewhere.
pyspf==2.0.14
dnspython==2.6.1
pytest==9.0.3
pytest-asyncio==1.3.0
